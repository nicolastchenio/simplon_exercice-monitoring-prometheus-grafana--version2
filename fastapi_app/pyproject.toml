[project]
name = "fastapi-app"
version = "0.1.0"
description = "FastAPI application for monitoring with Prometheus"
dependencies = [
    "fastapi",
    "uvicorn",
    "prometheus-client",
    "psutil",
    "loguru",
    "python-dotenv",
    "python-multipart"
]

[tool.uv]
package = true
