global:
  scrape_interval: 5s # Récupération des métriques toutes les 5 secondes (demandé dans le README)

scrape_configs:
  - job_name: 'fastapi'
    static_configs:
      - targets: ['api:8080']

  - job_name: 'node-exporter'
    static_configs:
      - targets: ['node-exporter:9100']
